version: '3.8'
services:
  web:
    build: .
    entrypoint: /srv/entrypoint 0.0.0.0:80
    environment:
      BUILD_URL: "${BUILD_URL}"
      JENKINS_TOKEN: "${JENKINS_TOKEN}"
      FLASK_DEBUG: "${FLASK_DEBUG}"
    ports:
    - "${PORT}:80"
    volumes:
    - .:/srv
